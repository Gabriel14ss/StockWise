<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/iconoapp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gestiona1"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.Gestiona1.NoActionBar" />
        <activity
            android:name=".ui.empleado.Empleado"
            android:exported="false" />
        <activity
            android:name=".ui.pedido.Pedido"
            android:exported="false" />
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Gestiona" /> <!-- activity Registro -->
        <activity
            android:name=".ui.RegistroActivity"
            android:exported="true"
            android:parentActivityName=".ui.LoginActivity"
            android:theme="@style/Theme.Gestiona" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.Gestiona">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>